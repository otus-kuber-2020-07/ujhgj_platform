apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "backup-{{ name }}-pvc"
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: {{ storage_size }}
  selector:
    matchLabels:
      pv-usage: "backup-{{ name }}"
